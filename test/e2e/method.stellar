extern void printf(string fmt, ...)

class Box {
    i32 width
    i32 height

    void printArea() {
        printf("%d\n", this.width * this.height)
    }
}

new Box box
box.width = 10
box.height = 15
box.printArea()
