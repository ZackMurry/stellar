extern void printf(string fmt, ...)

class Container<T> {
    T val

    T getVal() {
        ret this.val
    }

    void setVal(T val) {
        this.val = val
    }
}

new Container<i32> intContainer
intContainer.setVal(12)
printf("%d\n", intContainer.getVal())

Container<f64> doubleContainer = new Container<f64>()
doubleContainer.setVal(13.37f64)
printf("%.2f\n", doubleContainer.getVal())
